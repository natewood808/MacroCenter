<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" asp-href-include="lib/bootstrap/dist/css/*.min.css" />
    <title>@ViewBag.Title</title>
    @* Elements that use the asp-validation-for tag helper and have an error gets
       added to this class. *@
    <style>
        .input-validation-error {
            border-color: red;
            background-color: #fee;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="card-header"><h4>@ViewBag.Title</h4></div>
            <div class="card-body">
                @* This is a message that is on the template layout to alert users of a 
                   successful edit of a record in the database. The message is set in 
                   the AdminController.cs class whenever a POST Edit action method is called. *@
                @if (TempData["message"] != null)
                {
                    <div class="alert alert-success">@TempData["message"]</div>
                }
                @RenderBody()
            </div>
        </div>
    </div>
</body>
</html>

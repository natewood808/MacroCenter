<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" asp-href-include="lib/bootstrap/dist/css/*.min.css" />
    <link rel="stylesheet" asp-href-include="Styles/login-styles.css" />
    <title>@ViewBag.Title</title>
    @* Elements that use the asp-validation-for tag helper and have an error gets
        added to this class. *@
    <style>
        .input-validation-error {
            border-color: red;
            background-color: #fee;
        }
    </style>
    @* These script elements use the tag helper to select the files from the libraries that
        help us handle client-side error validations. *@
    <script asp-src-include="lib/jquery/**/jquery.min.js"></script>
    <script asp-src-include="lib/jquery-validation/**/jquery.validate.min.js"></script>
    <script asp-src-include="lib/jquery-validation-unobtrusive/**/*.min.js"></script>
</head>
<body>
    <div class="login-container">
        <div class="card">
            <div class="card-header-login">
                <h4>@ViewBag.Title</h4>
                <a id="logout-button" class="btn btn-sm btn-primary float-right" asp-action="Logout" asp-controller="Account">Log Out</a>
            </div>
            <div class="card-body">
                @* This is a message that is on the template layout to alert users of a
                    successful edit of a record in the database. The message is set in
                    the AdminController.cs class whenever a POST Edit action method is called. *@
                @if (TempData["message"] != null)
                {
                    <div class="alert alert-success">@TempData["message"]</div>
                }
                @RenderBody()
            </div>
        </div>
    </div>
</body>
</html>

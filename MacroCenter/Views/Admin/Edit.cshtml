@model Product

@{ 
    ViewBag.Title = "Edit Product";
    Layout = "_AdminLayout";
}

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="ProductID" />
    <div class="form-group">
        <label asp-for="Name"></label>
        @* When applied to a span element, the asp-validation-for attribute applies a tag
           helper that will add a validation error message to the specified property (Product.Nmae)
           if there are any validation problems. It will add the error message to the span element,
           then add the element to the input-validation-error class, which makes it easy to 
           apply CSS styles to error message elements. *@
        <div><span asp-validation-for="Name" class="text-danger"></span></div> @* Handles displaying validation errors for specific form element. *@
        <input asp-for="Name" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Description"></label>
        <div><span asp-validation-for="Description" class="text-danger"></span></div> @* Handles displaying validation errors for specific form element. *@
        <textarea asp-for="Description" class="form-control"></textarea>
    </div>
    <div class="form-group">
        <label asp-for="Category"></label>
        <div><span asp-validation-for="Category" class="text-danger"></span></div> @* Handles displaying validation errors for specific form element. *@
        <input asp-for="Category" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Price"></label>
        <div><span asp-validation-for="Price" class="text-danger"></span></div> @* Handles displaying validation errors for specific form element. *@
        <input asp-for="Price" class="form-control" />
    </div>
    <div class="text-center">
        <button class="btn btn-primary admin_buttons" type="submit">Save</button>
        <a asp-action="Index" class="btn btn-default">Cancel</a>
    </div>
</form>